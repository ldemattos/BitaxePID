# bitaxe-pid-docker

## Task: Create a docker compose file to install bitaxepid

## Actions:

- Create a docker compose file that pulls the bitaxepid from github and run the bitaxepid application;
	- Link do bitaxepid application: https://github.com/ldemattos/BitaxePID;
	- Use minimal linux distribution x86 Alpine;
		- All python and other linux dependecies must be installed;
		- All input variables must be possible to be adjusted through docker environment variables;

